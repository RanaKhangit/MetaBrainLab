import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Contact MetaBrain Labs — Investor & Research Enquiries",
  description:
    "Contact MetaBrain Labs for investor enquiries, scientific collaboration, or institutional partnerships.",
};

export default function ContactLayout({ children }: { children: React.ReactNode }) {
  return children;
}
