import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Contact MetaBrain Lab — Investor & Research Enquiries",
  description:
    "Contact MetaBrain Lab for investor enquiries, scientific collaboration, or institutional partnerships.",
};

export default function ContactLayout({ children }: { children: React.ReactNode }) {
  return children;
}
